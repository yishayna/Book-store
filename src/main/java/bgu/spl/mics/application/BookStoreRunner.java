package bgu.spl.mics.application;

/** This is the Main class of the application. You should parse the input file,
 * create the different instances of the objects, and run the system.
 * In the end, you should output serialized objects.
 */
public class BookStoreRunner {
/*
    public static JsonHelper BookStoreParam;

    public static void main(String[] args) {
        String filePath="/home/yana/CLionProjects/Assignment2/assignment2version0.2/input.json";
        parseJson(filePath);

    }

    public static void start(){
        MessageBus bus = getinstance();
        activatedparam(BookStoreParam.customersservice,bus);
    }

    public static void activatedparam(ArrayList<JsonObject> param, MessageBus bus){

    }

    public static void parseJson(String path) {
        Gson gson = new Gson();
        FileReader fr;
        try {
            fr = new FileReader(path);
            BookStoreParam= gson.fromJson(fr, JsonHelper.class);
            System.out.println(BookStoreParam.resources);
        } catch (FileNotFoundException e) {
            e.printStackTrace();
        }
    }
    private class JsonHelper {
        @SerializedName("initialInventory")
        private ArrayList<JsonObject> inventories;
        @SerializedName("initialResources")
        private ArrayList<JsonObject> resources;

        @SerializedName("time")
        private ArrayList<JsonPrimitive>  timeservice;
        @SerializedName("selling")
        private ArrayList<JsonPrimitive>  sellingservice;
        @SerializedName("inventoryService")
        private ArrayList<JsonPrimitive>  inventoryServiceservice;
        @SerializedName("logistics")
        private ArrayList<JsonPrimitive>  logisticsservice;
        @SerializedName("resourcesService")
        private ArrayList<JsonPrimitive>  resourcesServiceservice;
        @SerializedName("customers")
        private ArrayList<JsonObject>  customersservice;

    }
*/
}